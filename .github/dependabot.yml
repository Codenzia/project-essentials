version: 2

# Dependabot intentionally disabled on this public Packagist mirror.
# This repo is a read-only mirror of the private project-essentials-dev
# repo (where version updates are managed). Keeping Dependabot active on
# both ends produces duplicate PRs that go nowhere.
#
# To re-enable, move the update rules from the private -dev repo here, or
# add ecosystem entries below.

updates: []
